#ifndef REMU_PICO_STDIO_H
#define REMU_PICO_STDIO_H

/* The bounded SDK examples include stdio for their documentation-facing API.
 * Output is captured through the native UART adapter instead of a hosted libc. */

#endif
