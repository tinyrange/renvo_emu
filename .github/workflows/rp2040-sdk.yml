name: RP2040 SDK qualification

on:
  schedule:
    - cron: "41 3 * * 0"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rp2040-sdk-qualification
  cancel-in-progress: true

jobs:
  sdk:
    name: Pinned Pico SDK UART/PWM slice
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Rust toolchain
        run: rustup show active-toolchain

      - name: Build pinned public toolchain image
        run: scripts/bootstrap-toolchains.sh

      - name: Build release interpreter
        run: cargo build --release -p remu-cli --locked

      - name: Run SDK qualification
        env:
          REMU_BIN: target/release/remu
        run: scripts/qualify-rp2040-sdk.sh

      - name: Upload SDK evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: rp2040-sdk-qualification
          path: |
            .remu/qualification/rp2040-sdk/
            qualification/rp2040-sdk.json
          if-no-files-found: warn
